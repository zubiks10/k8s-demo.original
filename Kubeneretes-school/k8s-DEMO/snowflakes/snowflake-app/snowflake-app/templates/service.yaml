apiVersion: v1
kind: Service
metadata:
  name: {{ include "snowflake-app.fullname" . }}
  labels:
    {{- include "snowflake-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "snowflake-app.selectorLabels" . | nindent 4 }}
