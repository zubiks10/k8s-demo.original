apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "snowflake-app.fullname" . }}-test-connection"
  labels:
    {{- include "snowflake-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "snowflake-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
